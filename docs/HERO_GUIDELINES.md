# Hero Guidelines

The README hero must remain readable at GitHub README width.

Rules:

- Prefer SVG with real text.
- No blur, tiny text, or busy decorative lines crossing labels.
- Keep the message obvious: files stay yours, agents get a database.
- Verify the SVG at roughly 1000px wide before shipping.
